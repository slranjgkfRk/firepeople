# Toss Securities Open API credentials.
# Issue them in the Toss Securities WTS: 설정 > Open API.
# Also register this machine's public IP under 설정 > Open API > 허용 IP 관리,
# otherwise every call is rejected with HTTP 403.
#
# Line 1: client_id     (observed prefix "tsck_live_")
# Line 2: client_secret (observed prefix "tssk_live_")
#
# The app itself never reads this file — keys are entered in onboarding and stored
# in the Keychain (README §7). This is only for local scripts and manual API checks.
tsck_live_xxxxxxxxxxxxxxxxxxxxxx
tssk_live_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
